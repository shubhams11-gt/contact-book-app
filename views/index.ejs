<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Simple Blog</title>
  <link rel="stylesheet" href="../style.css" />
</head>
<body>
  <header>
    <h1>All Blog Posts</h1>
  </header>
  <main>
    <div class="top-actions">
      <a class="create-button" href="/new">+ Create New Blog</a>
    </div>

    <% if (blogs.length === 0) { %>
      <p class="empty">No blogs yet!</p>
    <% } else { %>
      <% for (let blog of blogs) { %>
        <div class="blog-post">
          <h2><%= blog.title %></h2>
          <p><%= blog.content.substring(0, 200) %>...</p>
          <small>🗓 Posted on <%= blog.createdAt.toDateString() %></small>
          <br>
          <a class="view-link" href="/blog/<%= blog._id %>">Read More →</a>
        </div>
      <% } %>
    <% } %>
  </main>

</body>
</html>
